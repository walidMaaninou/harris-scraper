#!/bin/bash
set -e
# ensure pip is up-to-date (optional but safe)
python -m pip install --upgrade pip

# Download Playwright browser binaries (Chromium). This must run AFTER pip installs.
python -m playwright install chromium
